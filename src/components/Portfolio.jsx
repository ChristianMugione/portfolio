import styled from "styled-components";

export const Portfolio = () => {
  return (
    <StyledPortfolio>
      <h2>Portfolio</h2>
    </StyledPortfolio>
  );
};

const StyledPortfolio = styled.section`
  width: 100%;
  padding: 70px 0;
  color: #ffffffde;
`;
